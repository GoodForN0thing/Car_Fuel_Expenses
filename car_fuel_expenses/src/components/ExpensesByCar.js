import React, {useState, useContext} from 'react'
import { GlobalContext } from '../context/GlobalState'
import { Expenses } from './Expenses';


    const autot = refuels.map(refuel => refuel.car)
    const autot2 = [...new Set(autot)];


}